@AGENTS.md

Claude Code: the skill in `skills/cutlist/` installs as a plugin
(`/plugin marketplace add itsryanlenk/cutlist`, then `/plugin install cutlist@cutlist`)
or copies to `.claude/skills/cutlist/` with `bash install.sh --claude`.
Maintainers: read `CONTRIBUTING.md`, then `docs/DECISIONS.md`.
